@model WIMS.Models.TeamModels.TeamDetail

@{
    ViewData["Title"] = "Delete";
}

<div class="card mx-auto" id="team-delete">
    <div class="card-header table-header">        
        <h3>Are you sure you want to delete this team?</h3>
    </div>
    <div class="card-body ">
        <h4>Team Details</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.TeamId)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.TeamId)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.TeamName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.TeamName)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.ManagerName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.ManagerName)
            </dd>
            
            <dt class="col-sm-2">
                Employees
            </dt>
            <dd class="col-sm-10">
                @if (Model.EmployeeNames.Any())
                {
                    @for (int i = 0; i < Model.EmployeeNames.Count; i++)
                    {
                        <p class="m-0 p-0">@Model.EmployeeNames[i]</p>
                    }
                }
            </dd>
        </dl>
        <hr />


        <form asp-action="Delete">
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a class="btn btn-primary mx-1" style="width:auto" asp-controller="Team"
               asp-action="Index">Back To List</a>
            
        </form>
    </div>

</div>


